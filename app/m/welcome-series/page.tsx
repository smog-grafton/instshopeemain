import { WelcomeSeriesClient } from "./welcome-series-client";

export const metadata = {
  title: "New User Zone",
  description: "Welcome perks and deals for new users.",
};

export default function WelcomeSeriesPage() {
  return <WelcomeSeriesClient />;
}
